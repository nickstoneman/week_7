
<div class="container">
  <h1><%= @user.name %></h1>
  <p><%= image_tag(@user.picture, width: 200) %>
  <p><%= @user.bio %></p>
  <p><%= link_to @user.name, @user.url %></p>
    
    = link_to "http://www.example.com" do
   <strong>strong</strong>

  <p><%= @user.url %></p>
  <%= link_to "user's bog", @user.url, target: 'blank' %>


</div>